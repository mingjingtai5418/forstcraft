{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "install:linux": "npm install --no-optional"
  },
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.15"
  },
  "resolutions": {
    "rollup": "^4.0.0"
  }
}
